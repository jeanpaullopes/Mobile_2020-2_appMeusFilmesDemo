<?xml version="1.0" encoding="utf-8"?>
<!-- a sacada é transformar o XML em um layout colocando a tag abaixo depois de pronta
-->
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="categoria" type="br.edu.uniritter.mobile.appmeusfilmes.model.Categoria" android:id="@+id/varCategoria"/>
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="onClickButton1"
        android:orientation="vertical">

        <Button
            android:id="@+id/button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{categoria.descricao}" />

    </LinearLayout>
</layout>